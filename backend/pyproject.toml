[project]
name = "cadence-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "openai>=1.60,<3", "pydantic>=2.9,<3", "python-dotenv>=1,<2", "jinja2>=3.1,<4"]

[dependency-groups]
dev = ["pytest>=8,<10", "httpx>=0.27,<1"]

[tool.pytest.ini_options]
pythonpath = ["."]
